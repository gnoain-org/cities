pipeline:
  build:
    image: mhart/alpine-node:6
    mount:
      - /tmp/npm
    commands:
      - sudo chown ubuntu:ubuntu /tmp/npm
      - npm set cache /tmp/npm
      - npm install 
    environment:
      - PORT=6969
      - IP=localhost
      - NODE_ENV=development
